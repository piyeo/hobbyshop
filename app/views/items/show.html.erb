<% @page_title = @item.name %>
<%= @item.name %>
<%= @item.body %>
<%= @item.price %>
<%= @item.stock %>
<%= @item.release %>
<%= link_to "カートに追加する",:action => "add",:controller => "carts", :item_id => @item.id%>
