<% @page_title = @item.name %>
<%
score=0.0
if @item.reviews != []
  @item.reviews.each do |review|
    score += review.score
  end
  score = (score / @item.reviews.count).round(1)
end
%>
<%= render partial: 'shared/item/show/description', locals: { item: @item, score: score}%>
<%= render partial: 'shared/item/show/review_form', locals: { item: @item, review: @review}%>
<%= render partial: 'shared/item/show/review_index', locals: { reviews: @reviews}%>
